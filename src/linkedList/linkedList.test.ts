import * as ll from "./index"

describe("LinkedList", () => {
  test("size should be incremented and decremented depending on adding a new node ore removing a node from the linkedlist", () => {})
  test("adding a new node to the list correctly", () => {})
})
